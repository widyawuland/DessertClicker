<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.wulan.dessertclicker">

    <application
        android:icon="@mipmap/ic_dessert_clicker"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_dessert_clicker_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DessertClicker"
        tools:targetApi="32">
        <activity
            android:name="com.wulan.dessertclicker.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DessertClicker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>